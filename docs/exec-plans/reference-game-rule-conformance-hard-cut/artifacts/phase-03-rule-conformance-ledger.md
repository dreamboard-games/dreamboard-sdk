# Phase 03 Rule Conformance Ledger

Recorded: 2026-07-13 (Australia/Sydney).

Status: the focused normal-setup scenario matrices for Lantern Market, Last
Light, and Stormtrail are executed-passing. The coordinated packed SDK/public
CLI perspective proof remains a Phase 03 integration gate.

This ledger points from the approved prose bullets to executable evidence. It
does not assign rule IDs or add gameplay metadata to scenario source.

## Lantern Market

| Rule bullet opening text                                                    | Scenario or pure-test path                                                                                                    | Exact assertion/checkpoint                                                                                                                                                              | Result           |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| The deck has exactly 20 cards of each family                                | `test/scenarios/supported-player-count-{2,3,4,5}.scenario.ts`; `test/scenarios.test.ts`                                       | Setup reassembles the one shuffled deck from private hands plus hidden remainder and asserts 20 Lanterns, 20 Tea Cups, 20 Festival Banners, and 60 unique IDs.                          | executed-passing |
| The same seed and seat order reproduce both rounds' hands                   | `test/scenarios/supported-player-count-{2,3,4,5}.scenario.ts`; `test/scenarios.test.ts`                                       | Seed 29 is replayed at every supported count; round-one hands and round-two fresh deals are compared with the original shuffled order.                                                  | executed-passing |
| Every player receives six private cards at each round start                 | `test/scenarios/supported-player-count-{2,3,4,5}.scenario.ts`; `test/scenarios/projection-privacy.scenario.ts`                | Each owner sees six IDs, other players and spectator see counts only, and undealt IDs remain hidden at both round starts.                                                               | executed-passing |
| One sealed card per player resolves as a simultaneous reveal                | `test/scenarios/barrier-actionability.scenario.ts`; `test/scenarios.test.ts`                                                  | `given:0` through `given:3` prove one commitment per player, no public card before the last commitment, one atomic reveal, and the exact one-left hand rotation.                        | executed-passing |
| Committed players cannot act twice or inspect another locked choice         | `test/scenarios/barrier-actionability.scenario.ts`; `test/scenarios/projection-privacy.scenario.ts`; `test/scenarios.test.ts` | A committed seat has no actions, duplicate submit rejects `ALREADY_SUBMITTED`, sealed IDs are absent from waiting-player and spectator JSON, and waiters/blockers contain no self edge. | executed-passing |
| A full normal playthrough produces six picks in each of two rounds          | `test/scenarios/complete-game.scenario.ts`; supported-player-count scenario family                                            | Seed 7 completes 24 commands for two players; the family completes 24/36/48/60 commands for two through five players and reaches `gameOver` after twelve picks per player.              | executed-passing |
| Singles, multiple pairs, multiple triples, and leftover cards score exactly | `test/scenarios/round-scoring.scenario.ts`; `test/unit/scoring.test.ts`                                                       | Legal round history plus pure family cases assert Lantern singles, Tea Cup pairs, Banner triples, multiple complete sets, ignored leftovers, and no cross-family sets.                  | executed-passing |
| Round 1 cards clear without losing score or public history                  | `test/scenarios/complete-game.scenario.ts`; `test/scenarios/round-scoring.scenario.ts`; `test/scenarios.test.ts`              | `given:12` is round 2 pick 1 with round-one stalls cleared, score/history retained, and the next cards dealt from the original undealt deck.                                            | executed-passing |
| Sole winners, tied winners, and lower-place ranking ties publish            | `test/scenarios/outcome-{sole-winner,tied-winner,lower-place-tie}.scenario.ts`; `test/unit/scoring.test.ts`                   | Seeds 7, 8, and 15 assert descending standings, competition ranks, win/draw/loss labels, and the lower-place rank gap.                                                                  | executed-passing |
| Scenario setup may isolate rare branches                                    | All Lantern scenario files; `test/scenarios.test.ts`                                                                          | Every replay uses only `{ players, seed }`, accepted commands, and ordinary setup; no base, snapshot, state patch, injected deck, or test profile is referenced.                        | executed-passing |

## Last Light

| Rule bullet opening text                                                   | Scenario or pure-test path                                                                                                                              | Exact assertion/checkpoint                                                                                                                                                                  | Result           |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| normal setup has exactly one human and no opponent identity                | `test/scenarios/determinism-and-actors.scenario.ts`; `test/scenarios.test.ts`                                                                           | Setup exposes only seat 0; player inspect has one active human, spectator has no actions, and state/events/projections contain neither environment nor opponent identity.                   | executed-passing |
| one seeded shuffle of the exact eight-card weather composition             | `test/scenarios/complete-game.scenario.ts`; `test/scenarios.test.ts`                                                                                    | `setup:0` asserts all eight unique approved card IDs and exactly seven structured Fisher-Yates integer draws.                                                                               | executed-passing |
| `charge`, `repairBeacon`, and `reinforce` availability at their boundaries | `test/scenarios/availability-*.scenario.ts`; `test/scenarios.test.ts`                                                                                   | Inspect at cap, zero energy, stored reinforcement, and ordinary setup separates visible interactions from the exact executable action set.                                                  | executed-passing |
| charging caps at 7, repair spends 1, and reinforcement spends 2            | `test/scenarios/availability-charge.scenario.ts`; `availability-repair-no-energy.scenario.ts`; `availability-reinforce-no-energy.scenario.ts`           | Legal commands assert exact balances and probe `ENERGY_AT_CAP` / `NOT_ENOUGH_ENERGY` at the three boundaries.                                                                               | executed-passing |
| repair selection rejects a full or unknown beacon                          | `test/scenarios/availability-repair-beacon.scenario.ts`                                                                                                 | A level-two beacon rejects `BEACON_ALREADY_LIT`; the manifest-known non-beacon space rejects `UNKNOWN_BEACON`.                                                                              | executed-passing |
| immediate win on the third lit beacon with no later weather or countdown   | `test/scenarios/complete-game.scenario.ts`                                                                                                              | The seventh legal repair lights the last beacon, enters `gameOver`, publishes `ALL_BEACONS_LIT`, and leaves weather/countdown at the pre-command values.                                    | executed-passing |
| Calm preserves reinforcement                                               | `test/scenarios/weather-procedure-calm.scenario.ts`; `availability-reinforce-stored.scenario.ts`                                                        | Calm emits its exact event, advances countdown, and leaves a stored reinforcement available for a later dangerous card.                                                                     | executed-passing |
| Gale with and without reinforcement                                        | `test/scenarios/weather-procedure-gale.scenario.ts`; `weather-procedure-gale-reinforced.scenario.ts`                                                    | Separate normal seeded paths assert storm advance without defense and full prevention plus defense consumption with reinforcement.                                                          | executed-passing |
| each Squall target, including dimming and prevention of both effects       | `test/scenarios/weather-procedure-{north,harbor,south}-squall.scenario.ts`; matching `*-reinforced.scenario.ts` files                                   | Six replays cover all three targets with unblocked storm+dimming and reinforced prevention of both effects.                                                                                 | executed-passing |
| unblocked Squall effects finish before the storm terminal check            | `test/scenarios/complete-game-loss-storm.scenario.ts`                                                                                                   | The terminal South Squall records `storm-advanced` and then `beacon-dimmed` before `STORM_REACHED_LIGHTHOUSE`.                                                                              | executed-passing |
| storm loss takes precedence over countdown advancement                     | `test/scenarios/complete-game-loss-storm.scenario.ts`                                                                                                   | The final dangerous card reaches storm 6, publishes storm loss, leaves one turn remaining, and emits no later countdown event.                                                              | executed-passing |
| countdown loss after surviving weather                                     | `test/scenarios/complete-game-loss-dawn.scenario.ts`                                                                                                    | The final weather resolves without storm loss, countdown reaches zero, and `DAWN_ARRIVED` is the terminal reason.                                                                           | executed-passing |
| exact event ordering for every weather and countdown branch                | All `test/scenarios/weather-procedure-*.scenario.ts`; both complete loss scenarios                                                                      | Each scenario asserts the ordered event ID/payload sequence, including reinforcement consumption/preservation, effect events, and countdown position.                                       | executed-passing |
| identical weather, events, state, and outcome from the same seed           | `test/scenarios/determinism-and-actors.scenario.ts`; `test/scenarios.test.ts`                                                                           | Two independent replays compare full trusted state, player projection, diagnostics/events, and terminal outcome byte-for-byte structurally.                                                 | executed-passing |
| complete seeded games demonstrating both a win and each loss reason        | `test/scenarios/complete-game.scenario.ts`; `complete-game-loss-storm.scenario.ts`; `complete-game-loss-dawn.scenario.ts`                               | Seeds 3, 4, and 3 respectively reach the three distinct terminal reasons through seven/eight accepted commands from normal setup.                                                           | executed-passing |
| pointer and keyboard play with an intelligible event history               | `test/ui-scenarios/{initial.desktop,developing.mobile,reinforcement-hit.desktop,terminal.mobile}.scenario.ts`; `test/ui-scenarios/ui-scenarios.test.ts` | Desktop and 390x844 declarations derive opening, developed, reinforced-hit, and terminal views with `GameEventLog`, pointer/touch, and keyboard capabilities from legal replay checkpoints. | executed-passing |

## Stormtrail

| Rule bullet opening text                                                     | Scenario or pure-test path                                                                                      | Exact assertion/checkpoint                                                                                                                                                                                                                                                               | Result           |
| ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| A complete seeded game from normal fixed-map setup                           | `test/scenarios/complete-game.scenario.ts`; `test/scenarios.test.ts`                                            | Seed 1 replays 111 accepted commands across 32 turns from ordinary three-player setup; the final command places player 2's fourth camp and enters `gameOver` immediately.                                                                                                                | executed-passing |
| Exact map identity, 24-intersection and 30-edge topology                     | `test/scenarios/topology-and-setup.scenario.ts`; `test/scenarios.test.ts`                                       | Static topology asserts the seven named hexes, 24 unique intersections, and 30 unique edges; replay checkpoints assert seat-order camp/trail domains, occupied-target rejection, adjacency, and each crew's exact starting supplies.                                                     | executed-passing |
| Production for each terrain type, multiple adjacent camps                    | `test/scenarios/production.scenario.ts`; `test/scenarios.test.ts`                                               | Seed 1 reaches totals 4/5/6/8/9/10, 2/3/11/12, a Bandits-suppressed 5, and owned-camp multiplicities of two and three through 219 accepted commands.                                                                                                                                     | executed-passing |
| A 7 with no required discards and a 7 with multiple required players         | `test/scenarios/bandits.scenario.ts`; `test/scenarios/discard-barrier.scenario.ts`; `test/scenarios.test.ts`    | Seed 2 skips directly to `moveBandits` with no obligation; seed 1 reaches totals 8 and 9, exact private discards of four in both response orders, duplicate/wrong-count rejection, active/pending actors, the turn-owner waiter, and one derived non-self `blockedBy` edge.              | executed-passing |
| Legal and illegal Bandits destinations, no-victim movement                   | `test/scenarios/bandits.scenario.ts`; `test/scenarios/projection-privacy.scenario.ts`; `test/scenarios.test.ts` | Explore returns the exact dependent commands for no-, one-, and two-victim hexes while omitting the current hex; probes reject wrong cardinality, and two seed-identical replays steal the same supply with participant-only type visibility.                                            | executed-passing |
| Trail connectivity, interruption by an opponent camp                         | `test/scenarios/network-and-costs.scenario.ts`; `test/scenarios.test.ts`                                        | A 76-turn legal replay pays for and places all ten trails; separate checkpoints prove opponent-camp interruption, own-network continuation, occupied/disconnected/cost rejection, camp connectivity, exact atomic payment, and piece exhaustion.                                         | executed-passing |
| Valid and invalid 3:1 Supply Depot trades                                    | `test/scenarios/depot-trades.scenario.ts`; `test/scenarios.test.ts`                                             | One legal main phase performs three consecutive Timber-to-Brick exchanges with exact inventory deltas; same-resource and unaffordable probes reject without changing the checkpoint digest.                                                                                              | executed-passing |
| Bilateral rejection, acceptance, unaffordable and stale-acceptance rejection | `test/scenarios/bilateral-trade.scenario.ts`; `test/scenarios.test.ts`                                          | Pending inspect exposes only the target's two responses, the offeror waiter blocked by that target, and no third actor; reject and accept both resume the offeror, acceptance transfers both maps atomically, and unaffordable plus post-response stale accepts reject without mutation. | executed-passing |
| Immediate victory on the fourth camp, correct final ranks                    | `test/scenarios/complete-game.scenario.ts`; `test/scenarios.test.ts`                                            | The 111th accepted command pays and places the fourth camp, publishes player 2 rank 1 and both opponents rank 2, leaves the winning build last in history, and exposes no interaction for any seat.                                                                                      | executed-passing |
| Player projections proving private inventory composition                     | `test/scenarios/projection-privacy.scenario.ts`; `test/scenarios.test.ts`                                       | Three player inspections and spectator inspection compare owner-only supply maps and discard maps, participant-only stolen type, shared supply counts, public offer terms, and absence of trusted table/private state from spectator JSON.                                               | executed-passing |
| Action discovery showing only legal actors, targets, and dependent inputs    | `test/scenarios/topology-and-setup.scenario.ts`; all focused scenario files; `test/scenarios.test.ts`           | Setup exposes 24 then three exact topology targets to the correct seat; roll, main, discard, Bandits, and pending-trade checkpoints expose only actor-scoped legal actions; Bandits explore proves optional dependent victim omission; terminal exposes none.                            | executed-passing |
