import { supportedPlayerCountScenario } from "./supported-player-count.ts";
import { SUPPORTED_4_COMMANDS } from "./commands.ts";

export default supportedPlayerCountScenario(4, SUPPORTED_4_COMMANDS);
