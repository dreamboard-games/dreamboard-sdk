import { supportedPlayerCountScenario } from "./supported-player-count.ts";
import { SUPPORTED_5_COMMANDS } from "./commands.ts";

export default supportedPlayerCountScenario(5, SUPPORTED_5_COMMANDS);
