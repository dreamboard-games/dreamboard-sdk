import { supportedPlayerCountScenario } from "./supported-player-count.ts";
import { SUPPORTED_3_COMMANDS } from "./commands.ts";

export default supportedPlayerCountScenario(3, SUPPORTED_3_COMMANDS);
