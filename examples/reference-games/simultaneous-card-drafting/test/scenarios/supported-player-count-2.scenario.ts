import { supportedPlayerCountScenario } from "./supported-player-count.ts";
import { SUPPORTED_2_COMMANDS } from "./commands.ts";

export default supportedPlayerCountScenario(2, SUPPORTED_2_COMMANDS);
