import { defineScenario } from "../testing-types.ts";

export default defineScenario({
  id: "automa-river-rival.claim-cargo.terminal",
  description: "Six rounds produce cooperative win, draw, or loss evidence.",
  tags: ["cooperative-outcome", "terminal"],
  claimId: "terminal-claim",
});
