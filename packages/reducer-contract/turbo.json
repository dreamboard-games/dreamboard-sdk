{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "schema/**", "scripts/**", "fixtures/**"],
      "outputs": ["generated/**", "src/bundle.ts", "dist/**"]
    }
  }
}
