# @dreamboard-games/api-client

Generated public TypeScript client for the Dreamboard API.

This package is part of the fixed-version `@dreamboard-games/*` SDK release
train. It is generated from the checked-in public OpenAPI document in this
repository.

Typical imports:

```ts
import { createClient } from "@dreamboard-games/api-client/client.gen";
import type { HostSessionSnapshot } from "@dreamboard-games/api-client";
```

Use `@dreamboard-games/sdk` to inspect the coordinated package-set version.
