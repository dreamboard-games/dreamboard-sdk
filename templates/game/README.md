# Reducer authoring template

This is a reducer-only SDK fixture demonstrating ordinary manifest imports and
the five-member reducer runtime bundle. It does not include a browser UI or a
development server. Complete browser project scaffolds belong to the public
dev-host tooling.

Run `pnpm check` from this directory to typecheck and test the reducer.
